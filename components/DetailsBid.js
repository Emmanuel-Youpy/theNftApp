import React from "react";
import { StyleSheet, Text, View } from "react-native";

const DetailsBid = ({ navigation }) => {
  return (
    <View>
      <Text>okay</Text>
    </View>
  );
};

export default DetailsBid;

const styles = StyleSheet.create({});
